<?xml version="1.0" encoding="utf-8"?>
<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	package="ru.barsk.moc">
	<uses-permission android:name="android.permission.VIBRATE"/>
	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:theme="@style/AppTheme">
		<activity
			android:name=".MainActivity"
			android:label="@string/app_name">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>
		<activity
			android:name=".StarViewActivity"
			android:label="@string/app_name"/>
		<activity
			android:name=".ShipViewActivity"
			android:label="@string/app_name"/>
		<activity
			android:name=".EconomyActivity"
			android:label="@string/app_name"/>
		<activity
			android:name=".MapViewActivity"
			android:label="@string/app_name"/>
		<activity
			android:name=".EventsViewActivity"
			android:label="@string/app_name"/>
		<activity
			android:name=".CombatActivity"
			android:label="@string/app_name"/>
		<activity
			android:name=".BuildingsViewActivity"
			android:label="@string/app_name"/>
		<activity
			android:name=".ConstructionSelectionActivity"
			android:label="@string/app_name"/>
		<activity
			android:name=".ConstructionQueueActivity"
			android:label="@string/app_name"/>
		<activity
			android:name=".ScienceActivity"
			android:label="@string/app_name"/>
	</application>
</manifest>
